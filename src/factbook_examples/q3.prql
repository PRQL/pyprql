#SELECT name
#FROM facts
#WHERE population = (SELECT MIN(population) FROM facts);

func min column table = (
    "{column} = (SELECT MIN({column}) FROM {table})"
)
from facts
select name
filter population | min "facts"